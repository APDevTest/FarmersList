class Cart{
    constructor(products, total){
        this.products = [];
        this.total = 0;
    };

    addProduct(Product){
        this.products.push(product);
        this.total++;
        return this.total;
    }
    
}

